{
  "name": "microduck-policy-playground",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build && cp dist/index.html ../index.html",
    "check": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vite": "^7.0.0",
    "vite-plugin-singlefile": "^2.1.0"
  },
  "dependencies": {
    "@huggingface/hub": "^2.17.2"
  }
}
